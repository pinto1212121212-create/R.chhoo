{
  "name": "hanhac-uriel-pinto",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node test/e2e.mjs && node test/scripts.mjs",
    "test:cloud": "node test/rules.mjs && node test/cloud.mjs",
    "emulators": "cd firebase && npx firebase-tools emulators:start --project demo-hanhac --only auth,firestore,storage",
    "setup:cloud": "node scripts/setup-cloud.mjs",
    "verify:cloud": "node scripts/verify-cloud.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "firebase-tools": "^15.28.1"
  }
}
